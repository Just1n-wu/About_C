#pragma once
#include <stdio.h>

void swap(int *a, int *b);  // 交换位置
void Print(int *arr, int length); // 打印

void bubble_sort(int arr[], int length);  // 冒泡
void select_sort(int arr[], int length);  // 选择
void insert_sort(int arr[], int length);  // 插入